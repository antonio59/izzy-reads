# Production build env — committed on purpose (non-secret).
# Vite loads this automatically on `vite build` (production mode).
# This is the PRODUCTION Convex deployment — NOT the dev one in .env.local.
VITE_CONVEX_URL=https://loyal-vulture-39.convex.cloud
